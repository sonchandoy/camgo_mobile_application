import 'package:flutter/material.dart';
class LineVerticalWidget extends StatelessWidget {
  const LineVerticalWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 1,
      color: Colors.grey,
    );
  }
}
